sp {apply*accept-motion-plan
	(state <s> ^superstate nil 
			   ^name motion-control
               ^operator.name accept-motion-plan
               ^io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^postprocess <cmd>)
    (<cmd> ^status finished
           ^success true)
-->
	(<ol> ^postprocess <cmd> - 
		  ^execute <ex>)
 	(<ex> ^speed 0.9)
}