sp {apply*accept-motion-plan
	(state <s> ^superstate nil 
			   ^name motion-control
               ^operator.name accept-motion-plan
               ^request.speed <sp>
               ^io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^postprocess <cmd>)
    (<cmd> ^status finished
           ^success true)
-->
	(<ol> ^postprocess <cmd> - 
		  ^execute <ex>)
 	(<ex> ^speed <sp>)
}