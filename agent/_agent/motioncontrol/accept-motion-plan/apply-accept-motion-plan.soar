sp {apply*accept-motion-plan
	(state <s> ^name
               ^operator.name accept-motion-plan
               ^request.speed <sp>
               ^topstate.io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^postprocess <cmd>)
    (<cmd> ^status finished
           ^success true)
-->
	(<ol> ^postprocess <cmd> - 
		  ^execute <ex>)
 	(<ex> ^speed <sp>)
}