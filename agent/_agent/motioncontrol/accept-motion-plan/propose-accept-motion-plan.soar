sp {propose*accept-motion-plan
	(state <s> ^name
               ^request
               ^topstate.io.output-link <ol>)
  	(<ol> ^postprocess <p>)
   	(<p> ^success true)
-->
	(<s> ^operator <op> + =)
 	(<op> ^name accept-motion-plan)
 }