sp {propose*accept-motion-plan
	(state <s> ^superstate nil
               ^name motion-control
               ^plan-requested true
               ^io.input-link <il>)
 	(<il> ^planner <pl>)
  	(<pl> ^last-completed search
          ^success true)
-->
	(<s> ^operator <op> + =)
 	(<op> ^name accept-motion-plan)
 }