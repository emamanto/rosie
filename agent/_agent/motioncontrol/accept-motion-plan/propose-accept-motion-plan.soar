sp {propose*accept-motion-plan
	(state <s> ^superstate nil
               ^name motion-control
               ^plan-requested true
               ^io.input-link <il>)
 	(<il> ^planner.search <search>)
  	(<search> ^found true)
-->
	(<s> ^operator <op> + =)
 	(<op> ^name accept-motion-plan)
 }