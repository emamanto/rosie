sp {apply*postprocess-motion-plan
	(state <s> ^superstate nil 
			   ^name motion-control
               ^operator.name postprocess-motion-plan
               ^io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^search <cmd>)
    (<cmd> ^status finished
           ^success true)
-->
	(<ol> ^search <cmd> - 
		  ^postprocess <ex>)
 	(<ex> ^precision 0.5)
}