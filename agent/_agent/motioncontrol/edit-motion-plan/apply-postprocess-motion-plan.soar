sp {apply*postprocess-motion-plan
	(state <s> ^name
               ^request
               ^operator.name postprocess-motion-plan
               ^topstate.io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^plan <cmd>)
-->
	(<ol> ^plan <cmd> - 
		  ^postprocess <ex>)
 	(<ex> ^precision 0.5)
}

sp {apply*postprocess-motion-plan-if-continued
	(state <s> ^name
			   ^request
               ^operator.name postprocess-motion-plan
               ^topstate.io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^continue <cmd>)
-->
	(<ol> ^continue <cmd> -)
}