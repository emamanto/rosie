sp {apply*finish-motion
	(state <s> ^superstate nil 
			   ^name motion-control
               ^operator.name finish-motion
               ^request <r>
               ^io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^execute <cmd>)
    (<cmd> ^status finished
		   ^success true)
-->
	(<s> ^request <r> -)
	(<ol> ^execute <cmd> -)
}