sp {apply*finish-motion
	(state <s> ^name
               ^operator.name finish-motion
               ^request <r>
               ^topstate.io <io>)
  	(<io> ^output-link <ol>)
    (<ol> ^execute <cmd>)
    (<cmd> ^status finished
		   ^success true)
-->
	(<s> ^request <r> -)
	(<ol> ^execute <cmd> -)
}