sp {propose*interpret-motion-request
	(state <s> ^superstate nil
               ^name motion-control
               ^request
               ^io.output-link <ol>)
 	(<ol> -^search
          -^postprocess
          -^execute)
-->
	(<s> ^operator <op> + =)
 	(<op> ^name interpret-motion-request)
 }