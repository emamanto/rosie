sp {propose*interpret-motion-request
	(state <s> ^name
               ^request <r>
               ^topstate <ts>)
    (<ts> ^io.output-link <ol>)
    (<ol> -^plan.status finished
          -^plan.status stopped
          -^postprocess
          -^execute)
-->
	(<s> ^operator <op> + =)
 	(<op> ^name interpret-motion-request)
 }