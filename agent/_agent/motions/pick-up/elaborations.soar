# Copy down which object is supposed to be picked
# up and set up attending to scene changes at the
# right level.
sp {pick-up*elaborate*state
   (state <s> ^name pick-up
              ^superstate <ss>)
   (<ss> ^operator <sop>)
   (<sop> ^name pick-up
          ^pick-up-object <obj>)
   (<obj> ^id <id>)
-->
   (<s> ^pick-up-object <obj>
        ^attend-to-all-scene-changes true
        ^problemspace motion)
   (<ss> ^attend-to-all-scene-changes true -)
}