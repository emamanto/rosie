# Point operator
#
# It can be called as follows:
# sp {propose*point
#   (state <s> .... )
#-->
#   (<s> ^operator <o> +)
#   (<o> ^name point
#        ^pointed-object <some-obj>)
#}

source apply-point.soar
source elaborations.soar
#source propose-point.soar