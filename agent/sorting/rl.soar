sp {replay-last-action*propose*move-block
	(state <s> ^name replay-last-action
               ^action <a>
               ^command <cmd>
              -^replay done)
-->
	(<s> ^operator <o> +)
    (<o> ^name move-block)
}

sp {replay-last-action*propose*finish-replay
	(state <s> ^name replay-last-action
               ^replay done)
-->
	(<s> ^operator <o> +)
    (<o> ^name finish-replay)
}

sp {replay-last-action*apply*finish-replay
	(state <s> ^name replay-last-action
               ^superstate <ss>
               ^operator.name finish-replay)
-->
	(<ss> ^replay done)
}

sp {move-block*replay*propose*finish-replay
	(state <s> ^name move-block
               ^result-learned true)
-->
    (<s> ^operator <o> + >)
    (<o> ^name move-finish-replay)
}

sp {move-block*apply*finish-replay
	(state <s> ^name move-block
               ^superstate <ss>
               ^operator.name move-finish-replay)
-->
	(<ss> ^replay done)
}

sp {move-block*replay*apply*choose-move*success
   (state <s> ^name move-block
              ^reward-link <rl>
              ^block-volume <v>
              ^operator.name choose-move
              ^prev-successful true)
-->
   (<rl> ^reward.value 1)
   (<s> ^result-learned true)
   (write |Successful move reward|)
}

sp {move-block*replay*apply*choose-push*success
   (state <s> ^name move-block
              ^reward-link <rl>
              ^block-volume <v>
              ^operator.name choose-push
              ^prev-successful true)
-->
   (<rl> ^reward.value 1)
   (<s> ^result-learned true)
   (write |Successful push reward|)
}

sp {move-block*replay*apply*choose-move*fail
   (state <s> ^name move-block
              ^reward-link <rl>
              ^block-volume <v>
              ^operator.name choose-move
              ^prev-failure-reason grasping)
-->
   (<rl> ^reward.value -1)
   (<s> ^result-learned true)
   (write |Failed move reward, grasping|)
}

sp {move-block*replay*apply*choose-move*fail*not-grasp
   (state <s> ^name move-block
              ^block-volume <v>
              ^operator.name choose-move
              ^prev-failure-reason <> grasping)
-->
   #(<rl> ^reward.value -0.1)
   (<s> ^result-learned true)
   (write |Failed move with no reward, not grasping|)
}

sp {move-block*replay*apply*choose-push*fail
   (state <s> ^name move-block
              ^block-volume <v>
              ^operator.name choose-push
              ^prev-failure-reason <r>)
-->
   #(<rl> ^reward.value -0.1)
   (<s> ^result-learned true)
   (write |Failed push with no reward|)
}

###############################################################

sp {move-block*select-move*1-0
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000001 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-1
   (state <s> ^name move-block
               ^block-volume { > 0.000001 <= 0.000008 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-2
   (state <s> ^name move-block
               ^block-volume { > 0.000008 <= 0.000027 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-3
   (state <s> ^name move-block
               ^block-volume { > 0.000027 <= 0.000064 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-4
   (state <s> ^name move-block
               ^block-volume { > 0.000064 <= 0.000125 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-5
   (state <s> ^name move-block
               ^block-volume { > 0.000125 <= 0.000216 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-6
   (state <s> ^name move-block
               ^block-volume { > 0.000216 <= 0.000343 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-7
   (state <s> ^name move-block
               ^block-volume { > 0.000343 <= 0.000512 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-8
   (state <s> ^name move-block
               ^block-volume { > 0.000512 <= 0.000729 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-9
   (state <s> ^name move-block
               ^block-volume { > 0.000729 <= 0.001 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-10
   (state <s> ^name move-block
               ^block-volume { > 0.001 <= 0.001331 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-11
   (state <s> ^name move-block
               ^block-volume { > 0.001331 <= 0.001728 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-12
   (state <s> ^name move-block
               ^block-volume { > 0.001728 <= 0.002197 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-13
   (state <s> ^name move-block
               ^block-volume { > 0.002197 <= 0.002744 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-14
   (state <s> ^name move-block
               ^block-volume { > 0.002744 <= 0.003375 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-15
   (state <s> ^name move-block
               ^block-volume { > 0.003375 <= 0.01 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

##

sp {move-block*select-move*2-1
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000008 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*2-2
   (state <s> ^name move-block
               ^block-volume { > 0.000008 <= 0.000064 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*2-3
   (state <s> ^name move-block
               ^block-volume { > 0.000064 <= 0.000216 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*2-4
   (state <s> ^name move-block
               ^block-volume { > 0.000216 <= 0.000512 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*2-5
   (state <s> ^name move-block
               ^block-volume { > 0.000512 <= 0.001 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*2-6
   (state <s> ^name move-block
               ^block-volume { > 0.001 <= 0.001728 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*2-7
   (state <s> ^name move-block
               ^block-volume { > 0.001728 <= 0.002744 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*2-8
   (state <s> ^name move-block
               ^block-volume { > 0.002744 <= 0.01 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

##

sp {move-block*select-move*3-1
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000064 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*3-2
   (state <s> ^name move-block
               ^block-volume { > 0.000064 <= 0.000512 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*3-3
   (state <s> ^name move-block
               ^block-volume { > 0.000512 <= 0.001728 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*3-4
   (state <s> ^name move-block
               ^block-volume { > 0.001728 <= 0.01 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

########################################################################

sp {move-block*select-push*1-0
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000001 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-1
   (state <s> ^name move-block
               ^block-volume { > 0.000001 <= 0.000008 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-2
   (state <s> ^name move-block
               ^block-volume { > 0.000008 <= 0.000027 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-3
   (state <s> ^name move-block
               ^block-volume { > 0.000027 <= 0.000064 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-4
   (state <s> ^name move-block
               ^block-volume { > 0.000064 <= 0.000125 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-5
   (state <s> ^name move-block
               ^block-volume { > 0.000125 <= 0.000216 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-6
   (state <s> ^name move-block
               ^block-volume { > 0.000216 <= 0.000343 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-7
   (state <s> ^name move-block
               ^block-volume { > 0.000343 <= 0.000512 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-8
   (state <s> ^name move-block
               ^block-volume { > 0.000512 <= 0.000729 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-9
   (state <s> ^name move-block
               ^block-volume { > 0.000729 <= 0.001 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-10
   (state <s> ^name move-block
               ^block-volume { > 0.001 <= 0.001331 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-11
   (state <s> ^name move-block
               ^block-volume { > 0.001331 <= 0.001728 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-12
   (state <s> ^name move-block
               ^block-volume { > 0.001728 <= 0.002197 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-13
   (state <s> ^name move-block
               ^block-volume { > 0.002197 <= 0.002744 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-14
   (state <s> ^name move-block
               ^block-volume { > 0.002744 <= 0.003375 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-15
   (state <s> ^name move-block
               ^block-volume { > 0.003375 <= 0.01 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

##

sp {move-block*select-push*2-1
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000008 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-2
   (state <s> ^name move-block
               ^block-volume { > 0.000008 <= 0.000064 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-3
   (state <s> ^name move-block
               ^block-volume { > 0.000064 <= 0.000216 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-4
   (state <s> ^name move-block
               ^block-volume { > 0.000216 <= 0.000512 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-5
   (state <s> ^name move-block
               ^block-volume { > 0.000512 <= 0.001 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-6
   (state <s> ^name move-block
               ^block-volume { > 0.001 <= 0.001728 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-7
   (state <s> ^name move-block
               ^block-volume { > 0.001728 <= 0.002744 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-8
   (state <s> ^name move-block
               ^block-volume { > 0.002744 <= 0.01 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

##

sp {move-block*select-push*3-1
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000064 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*3-2
   (state <s> ^name move-block
               ^block-volume { > 0.000064 <= 0.000512 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*3-3
   (state <s> ^name move-block
               ^block-volume { > 0.000512 <= 0.001728 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*3-4
   (state <s> ^name move-block
               ^block-volume { > 0.001728 <= 0.01 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}