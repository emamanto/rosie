sp {replay-last-action*propose*move-block
	(state <s> ^name replay-last-action
               ^action <a>
               ^command <cmd>
              -^replay done)
-->
	(<s> ^operator <o> +)
    (<o> ^name move-block)
}

sp {replay-last-action*propose*finish-replay
	(state <s> ^name replay-last-action
               ^replay done)
-->
	(<s> ^operator <o> +)
    (<o> ^name finish-replay)
}

sp {replay-last-action*apply*finish-replay
	(state <s> ^name replay-last-action
               ^superstate <ss>
               ^operator.name finish-replay)
-->
	(<ss> ^replay done)
}

sp {move-block*replay*propose*finish-replay
	(state <s> ^name move-block
               ^result-learned true)
-->
    (<s> ^operator <o> + >)
    (<o> ^name move-finish-replay)
}

sp {move-block*apply*finish-replay
	(state <s> ^name move-block
               ^superstate <ss>
               ^operator.name move-finish-replay)
-->
	(<ss> ^replay done)
}

sp {move-block*replay*apply*choose-move*success
   (state <s> ^name move-block
              ^reward-link <rl>
              ^block-volume <v>
              ^operator.name choose-move
              ^prev-successful true)
-->
   (<rl> ^reward.value 1)
   (<s> ^result-learned true)
   (write |Successful move reward|)
}

sp {move-block*replay*apply*choose-push*success
   (state <s> ^name move-block
              ^reward-link <rl>
              ^block-volume <v>
              ^operator.name choose-push
              ^prev-successful true)
-->
   (<rl> ^reward.value 1)
   (<s> ^result-learned true)
   (write |Successful push reward|)
}

sp {move-block*replay*apply*choose-move*fail
   (state <s> ^name move-block
              ^reward-link <rl>
              ^block-volume <v>
              ^operator.name choose-move
              ^prev-failure-reason grasping)
-->
   (<rl> ^reward.value -1)
   (<s> ^result-learned true)
   (write |Failed move reward, grasping|)
}

sp {move-block*replay*apply*choose-move*fail*not-grasp
   (state <s> ^name move-block
              ^block-volume <v>
              ^operator.name choose-move
              ^prev-failure-reason <> grasping)
-->
   #(<rl> ^reward.value -0.1)
   (<s> ^result-learned true)
   (write |Failed move with no reward, not grasping|)
}

sp {move-block*replay*apply*choose-push*fail
   (state <s> ^name move-block
              ^block-volume <v>
              ^operator.name choose-push
              ^prev-failure-reason <r>)
-->
   #(<rl> ^reward.value -0.1)
   (<s> ^result-learned true)
   (write |Failed push with no reward|)
}

###############################################################

sp {move-block*select-move*1-0
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000000000125}
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-1
   (state <s> ^name move-block
               ^block-volume { > 0.000000000125 <= 0.000003375}
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-2
   (state <s> ^name move-block
               ^block-volume { > 0.000003375 <= 0.000015625 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-3
   (state <s> ^name move-block
               ^block-volume { > 0.000015625 <= 0.000042875 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-4
   (state <s> ^name move-block
               ^block-volume { > 0.000042875 <= 0.000091125 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-5
   (state <s> ^name move-block
               ^block-volume { > 0.000091125 <= 0.000166375 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-6
   (state <s> ^name move-block
               ^block-volume { > 0.000166375 <= 0.000274625 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-7
   (state <s> ^name move-block
               ^block-volume { > 0.000274625 <= 0.000421875 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-8
   (state <s> ^name move-block
               ^block-volume { > 0.000421875 <= 0.000614125 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-9
   (state <s> ^name move-block
               ^block-volume { > 0.000614125 <= 0.000857375 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-10
   (state <s> ^name move-block
               ^block-volume { > 0.000857375 <= 0.001157625 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-11
   (state <s> ^name move-block
               ^block-volume { > 0.001157625 <= 0.001520875 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-12
   (state <s> ^name move-block
               ^block-volume { > 0.001520875 <= 0.001953125 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-13
   (state <s> ^name move-block
               ^block-volume { > 0.001953125 <= 0.002460375 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

sp {move-block*select-move*1-14
   (state <s> ^name move-block
               ^block-volume { > 0.002460375 <= 0.008 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.1)
}

##

sp {move-block*select-move*2-1
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000003375 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-move*2-2
   (state <s> ^name move-block
               ^block-volume { > 0.000003375 <= 0.000042875 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-move*2-3
   (state <s> ^name move-block
               ^block-volume { > 0.000042875 <= 0.000166375 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-move*2-4
   (state <s> ^name move-block
               ^block-volume { > 0.000166375 <= 0.000421875 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-move*2-5
   (state <s> ^name move-block
               ^block-volume { > 0.000421875 <= 0.000857375 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-move*2-6
   (state <s> ^name move-block
               ^block-volume { > 0.000857375 <= 0.001520875 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-move*2-7
   (state <s> ^name move-block
               ^block-volume { > 0.001520875 <= 0.008 }
               ^operator <o> +)
    (<o> ^name choose-move)
-->
	(<s> ^operator <o> = 0.0)
}
###############################################################

sp {move-block*select-push*1-0
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000000000125}
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-1
   (state <s> ^name move-block
               ^block-volume { > 0.000000000125 <= 0.000003375}
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-2
   (state <s> ^name move-block
               ^block-volume { > 0.000003375 <= 0.000015625 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-3
   (state <s> ^name move-block
               ^block-volume { > 0.000015625 <= 0.000042875 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-4
   (state <s> ^name move-block
               ^block-volume { > 0.000042875 <= 0.000091125 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-5
   (state <s> ^name move-block
               ^block-volume { > 0.000091125 <= 0.000166375 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-6
   (state <s> ^name move-block
               ^block-volume { > 0.000166375 <= 0.000274625 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-7
   (state <s> ^name move-block
               ^block-volume { > 0.000274625 <= 0.000421875 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-8
   (state <s> ^name move-block
               ^block-volume { > 0.000421875 <= 0.000614125 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-9
   (state <s> ^name move-block
               ^block-volume { > 0.000614125 <= 0.000857375 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-10
   (state <s> ^name move-block
               ^block-volume { > 0.000857375 <= 0.001157625 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-11
   (state <s> ^name move-block
               ^block-volume { > 0.001157625 <= 0.001520875 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-12
   (state <s> ^name move-block
               ^block-volume { > 0.001520875 <= 0.001953125 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-13
   (state <s> ^name move-block
               ^block-volume { > 0.001953125 <= 0.002460375 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*1-14
   (state <s> ^name move-block
               ^block-volume { > 0.002460375 <= 0.008 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

##

sp {move-block*select-push*2-1
   (state <s> ^name move-block
               ^block-volume { > 0.000 <= 0.000003375 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-2
   (state <s> ^name move-block
               ^block-volume { > 0.000003375 <= 0.000042875 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-3
   (state <s> ^name move-block
               ^block-volume { > 0.000042875 <= 0.000166375 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-4
   (state <s> ^name move-block
               ^block-volume { > 0.000166375 <= 0.000421875 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-5
   (state <s> ^name move-block
               ^block-volume { > 0.000421875 <= 0.000857375 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-6
   (state <s> ^name move-block
               ^block-volume { > 0.000857375 <= 0.001520875 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}

sp {move-block*select-push*2-7
   (state <s> ^name move-block
               ^block-volume { > 0.001520875 <= 0.008 }
               ^operator <o> +)
    (<o> ^name choose-push)
-->
	(<s> ^operator <o> = 0.0)
}